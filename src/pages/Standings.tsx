import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { useGrid } from "@/context/GridContext";
import { drivers, standings, teamById } from "@/lib/gridData";

export default function Standings() {
  const { t } = useGrid();
  return <section className="mx-auto max-w-6xl px-4 py-12 sm:px-6 lg:px-8"><h1 className="font-display text-4xl sm:text-5xl font-black">{t.standings.title}</h1><p className="mt-3 text-muted-foreground">{t.standings.subtitle}</p><Tabs defaultValue="drivers" className="mt-8"><TabsList className="bg-muted"><TabsTrigger value="drivers">{t.common.drivers}</TabsTrigger><TabsTrigger value="constructors">{t.common.constructors}</TabsTrigger></TabsList><TabsContent value="drivers" className="mt-5 overflow-hidden rounded-lg border border-border bg-card"><div className="overflow-x-auto"><table className="w-full min-w-[480px] text-left"><thead className="bg-muted text-sm text-muted-foreground"><tr><th className="p-3 sm:p-4">{t.standings.position}</th><th className="p-3 sm:p-4">{t.standings.name}</th><th className="p-3 sm:p-4">{t.common.team}</th><th className="p-3 sm:p-4">{t.standings.points}</th><th className="p-3 sm:p-4">{t.standings.wins}</th></tr></thead><tbody>{standings.drivers.map((row) => { const driver = drivers.find((item) => item.id === row.driverId)!; const team = teamById(driver.teamId); return <tr key={row.driverId} className="border-t border-border transition-grid-theme hover:bg-accent"><td className="p-3 sm:p-4 font-black">{row.position}</td><td className="p-3 sm:p-4 font-semibold">{driver.name}</td><td className="p-3 sm:p-4"><span className="mr-2 inline-block h-3 w-3 rounded-full" style={{ backgroundColor: `hsl(${team.primary})` }} />{team.name}</td><td className="p-3 sm:p-4">{row.points}</td><td className="p-3 sm:p-4">{row.wins}</td></tr>; })}</tbody></table></div></TabsContent><TabsContent value="constructors" className="mt-5 overflow-hidden rounded-lg border border-border bg-card"><div className="overflow-x-auto"><table className="w-full min-w-[320px] text-left"><thead className="bg-muted text-sm text-muted-foreground"><tr><th className="p-3 sm:p-4">{t.standings.position}</th><th className="p-3 sm:p-4">{t.common.team}</th><th className="p-3 sm:p-4">{t.standings.points}</th></tr></thead><tbody>{standings.constructors.map((row) => { const team = teamById(row.teamId); return <tr key={row.teamId} className="border-t border-border transition-grid-theme hover:bg-accent"><td className="p-3 sm:p-4 font-black">{row.position}</td><td className="p-3 sm:p-4 font-semibold"><span className="mr-2 inline-block h-3 w-3 rounded-full" style={{ backgroundColor: `hsl(${team.primary})` }} />{team.name}</td><td className="p-3 sm:p-4">{row.points}</td></tr>; })}</tbody></table></div></TabsContent></Tabs></section>;
}
